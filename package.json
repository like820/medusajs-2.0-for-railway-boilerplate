{
    "name": "medusa-boilerplate-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "./node_modules/.bin/concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix storefront\" --names \"BACKEND,STOREFRONT\" --prefix-colors \"blue,magenta\"",
        "install:all": "npm install && npm install --prefix backend --legacy-peer-deps && npm install --prefix storefront"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}